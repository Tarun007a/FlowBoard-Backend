spring:
  application:
    name: flowboard-server

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
#    hostname: eureka
    prefer-ip-address: true